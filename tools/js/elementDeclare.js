const overlay = document.getElementById("infoOverlay");
const overlayTitle = document.getElementById("overlayTitle");
const overlayText = document.getElementById("overlayText");

const toolContainer = document.getElementById("modelContainer");
const toolContainerWrapper = document.getElementById("toolContainerWrapper");

const title = document.getElementById("title");

// friendly_rm.js
const rmInput = document.getElementById("rmInput");
const rmResults = document.getElementById("rmResults");